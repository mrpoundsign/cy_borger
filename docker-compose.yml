version: '3.8'

services:
  app:
    build: .
    ports:
      - "9090:8080"
    volumes:
      - cy_borger_data:/app/data
    restart: unless-stopped
    environment:
      - DB_PATH=/app/data/cy_borger.db

volumes:
  cy_borger_data:
